---
import type { Image } from "../../../lib/types";
import SanityImage from "../SanityImage.astro";

interface Props {
    node: Image;
}
const { node } = Astro.props;
---

<SanityImage src={node} width={768} />
